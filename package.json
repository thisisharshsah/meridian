{
  "name": "meridian",
  "private": true,
  "scripts": {
    "dev": "./dev.sh",
    "seed": "./seed.sh",
    "reset": "./reset.sh",
    "test": "cd server && cargo test",
    "typecheck": "cd apps/web && ./node_modules/.bin/tsc --noEmit",
    "build": "cd server && cargo build --release && cd ../apps/web && ./node_modules/.bin/next build"
  }
}
